# Example SMTP configuration for Yii2 mailer
SMTP_HOST=smtp.example.com
SMTP_PORT=587                  # Used by frontend config and controllers
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_USER=your_smtp_username   # Used by frontend config and controllers
SMTP_PASS=your_smtp_password   # Used by frontend config and controllers
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=your@email.com
SMTP_FROM_NAME=YourAppName

# Database connection for backend/web/test-db.php
DB_HOST=localhost
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASS=your_db_password

# Copy this file to .env and fill in your real credentials. .env is gitignored.

# URLs for frontend and backend (used in params.php)
FRONTEND_URL=your_frontend_url
BACKEND_URL=your_backend_url
